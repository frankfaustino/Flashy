import styled from '../../../utils'

const AuthWrapper = styled.div`
  display: grid;
  grid-auto-rows: 125px 100px 1fr;
  justify-content: center;
  align-items: center;
`

export default AuthWrapper